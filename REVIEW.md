# コードレビュー（2026-05-26）

対象: `index.html`, `script.js`, `style.css`, `README.md`

## 総評

見た目と目的が明快で、シンプルな構成で読みやすいです。特にUI構造とスコア表示は直感的です。
一方で、公開リポジトリ運用と実運用を想定すると、優先度の高い改善点がいくつかあります。

## 指摘事項

### 1. [High] APIキーのハードコード（公開情報化）
- `script.js` に APIキーが直接記載されており、公開リポジトリ上で第三者に利用されるリスクがあります。
- OpenWeatherの無料枠超過・不正利用・キー無効化の原因になります。

**提案**
- GitHub Pages向けの完全秘匿は難しいため、以下のいずれかを推奨:
  - サーバーレス関数を挟んでキーを隠蔽する
  - キー利用制限（ドメイン制限等）を可能な範囲で設定する
  - デモ用キーのレート制限/監視前提で定期ローテーションする

### 2. [High] 例外処理不足（通信失敗時にUXが崩れる）
- `fetch` 失敗、`data.list` 欠落、APIエラーコードなどのハンドリングがありません。
- 現状ではエラー時に画面更新が中途半端になり、利用者に原因が伝わりません。

**提案**
- `try/catch` と `res.ok` チェックを追加
- ローディング表示とエラーメッセージ表示を導入
- `data.cod !== "200"` のケースを明示処理

### 3. [Medium] README の数式説明と実装式の差分
- READMEでは `Dry Score = 2 × 気温 + 3 × 風速 − 0.8 × 湿度 − 0.5 × 降水確率` と説明
- 実装では最後に `+ 60` のオフセットが追加されています。

**提案**
- どちらかに統一（README追記 or 実装変更）
- 実装意図（0–100への正規化）を式の注釈で説明

### 4. [Medium] XSS耐性（外部データを innerHTML へ直接挿入）
- APIレスポンス文字列をテンプレート文字列で `innerHTML` 挿入しています。
- OpenWeather由来データで通常リスクは低いものの、防御としては弱いです。

**提案**
- DOM API（`createElement`, `textContent`）で要素構築
- 最低限、文字列エスケープ関数を経由

### 5. [Low] 表記ゆれ
- タイトルや文言に `せんたくびより` / `センタクびより` が混在。

**提案**
- ブランド表記を統一

## 良い点

- 単一責務に近い構造で学習プロジェクトとして理解しやすい
- レスポンシブ対応が最小限で整理されている
- スコアの最小/最大を clamp しており、表示上の破綻を防いでいる

## 優先アクション（推奨順）

1. APIキー対策
2. 通信エラー時のUI/例外処理
3. READMEと実装の算出式整合
4. `innerHTML` 依存の縮小
5. 表記統一
